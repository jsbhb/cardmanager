package com.card.manager.factory.base;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class BaseService {
	protected final Logger logger_base = LoggerFactory.getLogger(this.getClass());
}
